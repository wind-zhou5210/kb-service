# Dark Mode 主题切换 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为知识库前端添加暗色/亮色主题切换功能，顶栏按钮一键切换，状态持久化到 localStorage。

**Architecture:** Ant Design v5 `darkAlgorithm` 处理 antd 组件暗色 + `[data-theme="dark"]` CSS 选择器覆盖自定义变量，双轨并行。Zustand store 管理状态，`document.documentElement.dataset.theme` 挂载到 DOM 根节点驱动 CSS 变量切换。

**Tech Stack:** React 18, TypeScript, Zustand, Ant Design v5, CSS Variables

---

### Task 1: 创建主题 Zustand Store

**Files:**
- Create: `frontend/src/store/theme.ts`

**Step 1: 创建 store 文件**

```typescript
import { create } from 'zustand'

type Theme = 'light' | 'dark'

interface ThemeState {
  theme: Theme
  toggleTheme: () => void
}

function getInitialTheme(): Theme {
  const stored = localStorage.getItem('kb-theme')
  if (stored === 'dark' || stored === 'light') return stored
  return 'light'
}

function applyTheme(theme: Theme) {
  document.documentElement.dataset.theme = theme
}

// 初始化时立即应用，避免页面闪烁
applyTheme(getInitialTheme())

export const useTheme = create<ThemeState>((set) => ({
  theme: getInitialTheme(),
  toggleTheme: () =>
    set((state) => {
      const next: Theme = state.theme === 'light' ? 'dark' : 'light'
      localStorage.setItem('kb-theme', next)
      applyTheme(next)
      return { theme: next }
    }),
}))
```

**Step 2: 编译检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: PASS, no errors in theme.ts

**Step 3: Commit**

```bash
git add frontend/src/store/theme.ts
git commit -m "feat: add theme Zustand store with localStorage persistence"
```

---

### Task 2: 添加暗色 CSS 变量

**Files:**
- Modify: `frontend/src/index.css` — 文件末尾追加暗色变量

**Step 1: 在 index.css 末尾追加暗色主题 CSS 变量**

追加内容：

```css
/* ---- 暗色主题 ---- */
[data-theme="dark"] {
  --ink-900: #FAFAFA;
  --ink-800: #E4E4E7;
  --ink-700: #D4D4D8;
  --ink-600: #A1A1AA;
  --ink-500: #71717A;
  --ink-400: #52525B;
  --ink-300: #3F3F46;
  --ink-200: #27272A;
  --ink-100: #18181B;
  --ink-50:  #0A0A0B;

  --accent: #818CF8;
  --accent-press: #6366F1;
  --accent-tint: #1E1B4B;

  --danger: #F87171;
  --danger-tint: #1C1010;
  --success: #4ADE80;

  --md-color: #60A5FA;
  --html-color: #FB923C;

  --bg: #09090B;
  --surface: #18181B;
  --border: #27272A;
  --border-strong: #3F3F46;

  --shadow: 0 1px 2px rgba(0,0,0,0.3);
  --shadow-md: 0 2px 8px rgba(0,0,0,0.4);
}

/* 暗色下代码块背景保持深色，让正文区背景更暗 */
[data-theme="dark"] .md-body pre {
  background: #0A0A0B;
}

/* 暗色下 scrollbar */
[data-theme="dark"] ::-webkit-scrollbar-thumb {
  background: var(--ink-300);
}
[data-theme="dark"] ::-webkit-scrollbar-thumb:hover {
  background: var(--ink-400);
}

/* 暗色下 antd 原生元素跟随 */
[data-theme="dark"] body {
  background: var(--bg);
  color: var(--ink-900);
}

/* 暗色下登录左侧品牌区保持墨色不变（已经是深色） */
```

**设计原则：**
- 墨阶翻转：`--ink-900` ↔ `--ink-50`，`--ink-800` ↔ `--ink-100`，依此类推
- accent 提亮：暗色背景上 indigo 需更亮才有对比度
- surface/bg 下沉到接近纯黑但保留层次
- 语义色调暗以匹配暗色背景

**Step 2: 验证 CSS 语法**

Run: `cd frontend && npx tsc --noEmit`
Expected: PASS (CSS 不影响 TypeScript 编译)

**Step 3: Commit**

```bash
git add frontend/src/index.css
git commit -m "feat: add dark theme CSS variable overrides"
```

---

### Task 3: 接入 Ant Design darkAlgorithm

**Files:**
- Modify: `frontend/src/main.tsx:1-39` — ConfigProvider 读取 theme，动态切换 algorithm

**Step 1: 修改 main.tsx**

完整替换为：

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import { ConfigProvider, theme } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import 'antd/dist/reset.css'
import 'highlight.js/styles/github.css'
import 'katex/dist/katex.min.css'
import './index.css'
import App from './App'
import { useTheme } from './store/theme'

function Root() {
  const currentTheme = useTheme((s) => s.theme)

  return (
    <ConfigProvider
      locale={zhCN}
      theme={{
        algorithm: currentTheme === 'dark' ? theme.darkAlgorithm : theme.defaultAlgorithm,
        token: {
          colorPrimary: '#4F46E5',
          colorInfo: '#4F46E5',
          borderRadius: 6,
          fontFamily: "-apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif",
          fontSize: 14,
          colorBorder: currentTheme === 'dark' ? '#27272A' : '#E4E4E7',
        },
        components: {
          Button: { fontWeight: 500, borderRadius: 6 },
          Card: { borderRadiusLG: 8 },
          Modal: { borderRadiusLG: 8 },
          Input: { borderRadius: 6 },
          Menu: { borderRadiusLG: 6 },
        },
      }}
    >
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </ConfigProvider>
  )
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Root />
  </React.StrictMode>,
)
```

**关键变更：**
- ConfigProvider 从顶层拆出到 `Root` 组件，以便读取 Zustand store
- `algorithm` 根据 `currentTheme` 动态切换 `darkAlgorithm` / `defaultAlgorithm`
- `colorBorder` token 跟随主题适配

**Step 2: 编译检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: PASS

**Step 3: 本地预览**

Run: `cd frontend && npm run dev`
打开浏览器，确认 antd 组件（Button, Card, Modal 等）在暗色下正常渲染。

**Step 4: Commit**

```bash
git add frontend/src/main.tsx
git commit -m "feat: wire Ant Design darkAlgorithm to theme store"
```

---

### Task 4: 顶栏添加主题切换按钮

**Files:**
- Modify: `frontend/src/components/AppLayout.tsx:1-58` — 添加切换按钮

**Step 1: 修改 AppLayout.tsx**

在搜索框和用户头像之间插入切换按钮。完整修改：

```tsx
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Dropdown, Input, Button } from 'antd'
import { LogoutOutlined, SearchOutlined, SunOutlined, MoonOutlined } from '@ant-design/icons'
import { useAuth } from '../store/auth'
import { useTheme } from '../store/theme'

interface Props {
  children: React.ReactNode
}

export default function AppLayout({ children }: Props) {
  const navigate = useNavigate()
  const logout = useAuth((s) => s.logout)
  const [q, setQ] = useState('')
  const { theme: currentTheme, toggleTheme } = useTheme((s) => ({
    theme: s.theme,
    toggleTheme: s.toggleTheme,
  }))

  const goSearch = () => {
    const trimmed = q.trim()
    navigate(trimmed ? `/search?q=${encodeURIComponent(trimmed)}` : '/search')
  }

  return (
    <div style={{ minHeight: '100vh', display: 'flex', flexDirection: 'column' }}>
      <header className="app-header">
        <div className="logo" onClick={() => navigate('/')}>
          <div className="logo-mark">K</div>
          <span>文件知识库</span>
        </div>
        <Input
          allowClear
          prefix={<SearchOutlined style={{ color: 'var(--ink-300)', fontSize: 13 }} />}
          placeholder="搜索文档..."
          value={q}
          onChange={(e) => setQ(e.target.value)}
          onPressEnter={goSearch}
          onFocus={(e) => e.target.select()}
          style={{ maxWidth: 280, height: 32, borderRadius: 6, marginLeft: 8 }}
        />
        <div style={{ flex: 1 }} />
        <Button
          type="text"
          icon={currentTheme === 'dark' ? <SunOutlined /> : <MoonOutlined />}
          onClick={toggleTheme}
          title={currentTheme === 'dark' ? '切换亮色模式' : '切换暗色模式'}
          style={{ color: 'var(--ink-500)' }}
        />
        <Dropdown
          menu={{ items: [{ key: 'logout', icon: <LogoutOutlined />, label: '退出登录', onClick: logout }] }}
          placement="bottomRight"
        >
          <div
            style={{
              width: 28, height: 28, borderRadius: 6,
              background: 'var(--ink-100)', display: 'flex',
              alignItems: 'center', justifyContent: 'center',
              cursor: 'pointer', fontSize: 12, fontWeight: 600, color: 'var(--ink-600)',
            }}
          >
            A
          </div>
        </Dropdown>
      </header>
      <div style={{ flex: 1 }} className="page-fade">{children}</div>
    </div>
  )
}
```

**变更说明：**
- 新增 `SunOutlined` / `MoonOutlined` 图标导入
- 新增 `useTheme` hook 读取当前主题和切换函数
- 搜索框右侧、用户头像左侧插入 `Button type="text"` 切换按钮
- 暗色时显示太阳图标（点击切回亮色），亮色时显示月亮图标（点击切暗色）

**Step 2: 编译检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: PASS

**Step 3: Commit**

```bash
git add frontend/src/components/AppLayout.tsx
git commit -m "feat: add theme toggle button to header"
```

---

### Task 5: 端到端验证

**Step 1: 启动开发服务器**

Run: `cd frontend && npm run dev`
打开 `http://localhost:5173`

**Step 2: 手动验证清单**

| 检查项 | 预期 |
|--------|------|
| 首次加载为亮色主题 | 白色背景、深色文字 |
| 点击月亮图标 | 整个页面切换为暗色 |
| antd Button | 暗色风格、无样式错乱 |
| antd Card | 暗色风格 |
| antd Dropdown | 暗色下拉菜单 |
| 自定义 .col-card | border/背景为暗色 |
| .doc-item hover | 暗色 hover 效果 |
| 搜索框 Input | 暗色输入框 |
| 刷新页面 | 保持暗色（localStorage 持久化） |
| 再次点击太阳图标 | 切回亮色 |
| 登录页 | 暗色/亮色均正常 |

**Step 3: 验证编译无错误**

Run: `cd frontend && npm run build`
Expected: PASS, build success

**Step 4: 最终 Commit**

```bash
# 如有任何微调提交
git add -A
git commit -m "chore: final dark mode polish"
```

---

### 文件变更总览

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/src/store/theme.ts` | 新建 | Zustand theme store，localStorage 持久化 |
| `frontend/src/index.css` | 修改 | 末尾追加 `[data-theme="dark"]` 暗色变量 |
| `frontend/src/main.tsx` | 修改 | ConfigProvider 迁入 Root 组件，动态 darkAlgorithm |
| `frontend/src/components/AppLayout.tsx` | 修改 | 顶栏搜索框右侧添加主题切换按钮 |
