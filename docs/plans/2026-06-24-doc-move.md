# 文档移动功能 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现文档在集合间移动，通过 Dropdown 菜单触发，Modal 展示集合卡片选择目标，保留全部元数据和分享链接

**Architecture:** 后端新增 `POST /api/documents/{doc_id}/move` 端点，更新 `collection_id` + FTS 索引 + 集合时间戳。前端在 Dropdown 加「移动到...」菜单项，复用已有 `api.listCollections()` 加载集合列表，卡片式 Modal 选择目标

**Tech Stack:** FastAPI + SQLModel + React 18 + Ant Design 5

---

### Task 1: 后端 — 新增文档移动 API 端点

**Files:**
- Modify: `backend/app/api/documents.py:178`（在 `update_document` 之后插入）

**Step 1: 新增 Request Body Model**

在 `class DocumentUpdate` 定义之后，新增：

```python
class DocumentMove(BaseModel):
    collection_id: int
```

**Step 2: 新增移动端点**

在 `@router.patch("/documents/{doc_id}")` 和 `@router.delete("/documents/{doc_id}")` 之间插入：

```python
@router.post("/documents/{doc_id}/move")
async def move_document(
    doc_id: int,
    body: DocumentMove,
    session: Annotated[AsyncSession, Depends(get_session)],
    user: CurrentUser,
):
    doc = await session.get(Document, doc_id)
    if not doc:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "文件不存在")

    if doc.collection_id == body.collection_id:
        raise HTTPException(status.HTTP_400_BAD_REQUEST, "已在当前集合中")

    target_col = await session.get(Collection, body.collection_id)
    if not target_col:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "目标集合不存在")

    old_col_id = doc.collection_id
    old_col = await session.get(Collection, old_col_id)

    # 更新归属
    doc.collection_id = body.collection_id
    doc.updated_at = datetime.now(timezone.utc)
    session.add(doc)

    # 更新新旧集合时间戳
    now = datetime.now(timezone.utc)
    if old_col:
        old_col.updated_at = now
        session.add(old_col)
    target_col.updated_at = now
    session.add(target_col)

    await session.commit()
    await session.refresh(doc)

    # 更新 FTS 索引中的集合名
    await session.execute(
        text("UPDATE fts_index SET collection_name = :name WHERE document_id = :doc_id"),
        {"name": target_col.name, "doc_id": doc_id},
    )
    await session.commit()

    return doc
```

**Step 3: 验证**

```bash
cd backend && python -m py_compile app/api/documents.py
# 预期: 无输出（编译通过）
```

**Step 4: Commit**

```bash
git add backend/app/api/documents.py
git commit -m "feat: add POST /api/documents/{id}/move endpoint"
```

---

### Task 2: 前端 — API 客户端新增 moveDocument 方法

**Files:**
- Modify: `frontend/src/api/client.ts`

**Step 1: 查找现有 API 方法区域，追加移动方法**

在 `deleteDocument` 方法下方新增：

```typescript
async moveDocument(docId: number, collectionId: number): Promise<DocumentItem> {
  const { data } = await client.post(`/documents/${docId}/move`, { collection_id: collectionId })
  return data
}
```

**Step 2: 验证**

```bash
cd frontend && npx tsc --noEmit
# 预期: 无类型错误
```

**Step 3: Commit**

```bash
git add frontend/src/api/client.ts
git commit -m "feat: add moveDocument API method to frontend client"
```

---

### Task 3: 前端 — CollectionDetail 新增移动功能

**Files:**
- Modify: `frontend/src/pages/CollectionDetail.tsx`

**Step 1: 导入新图标**

在现有图标导入行（约 line 3-8）的 `FolderOutlined` 后面追加 `SwapOutlined`：

```typescript
import {
  ArrowLeftOutlined, UploadOutlined, DeleteOutlined, DownloadOutlined,
  MoreOutlined, SearchOutlined, FileTextOutlined, Html5Outlined, FolderOutlined, EditOutlined,
  FullscreenOutlined, FullscreenExitOutlined, StopOutlined, SwapOutlined,
} from '@ant-design/icons'
```

**Step 2: 新增移动相关状态**

在现有状态声明区域（约 line 60-71）追加：

```typescript
const [moveModalOpen, setMoveModalOpen] = useState(false)
const [moveTarget, setMoveTarget] = useState<DocumentItem | null>(null)
const [collections, setCollections] = useState<Collection[]>([])
```

**Step 3: 新增打开移动 Modal 的方法**

在 `handleDocShare` 方法附近新增：

```typescript
const openMoveModal = async (doc: DocumentItem) => {
  setMoveTarget(doc)
  setMoveModalOpen(true)
  const cols = await api.listCollections()
  setCollections(cols)
}

const handleMove = async (targetColId: number) => {
  if (!moveTarget) return
  try {
    await api.moveDocument(moveTarget.id, targetColId)
    const targetCol = collections.find(c => c.id === targetColId)
    message.success(`已移动到「${targetCol?.name ?? targetColId}」`)
    setMoveModalOpen(false)
    setMoveTarget(null)
    if (selected?.id === moveTarget.id) {
      setSelected(null); setMdContent(''); setHtmlContent('')
    }
    loadDocs()
  } catch {
    message.error('移动失败，请重试')
  }
}
```

**Step 4: 在 Dropdown 菜单中加入「移动到...」**

修改 `dropdownItems` 函数（约 line 186），在「下载」之后、「取消分享」之前插入：

```typescript
{ key: 'move', label: '移动到...', icon: <SwapOutlined />, onClick: () => openMoveModal(doc) },
```

完整插入后的上下文：

```typescript
const dropdownItems = (doc: DocumentItem) => ({
  items: [
    { key: 'edit', label: '编辑详情', icon: <EditOutlined />, onClick: () => openEdit(doc) },
    { key: 'download', label: '下载', icon: <DownloadOutlined />, onClick: () => window.open(`/api/documents/${doc.id}/download`) },
    { key: 'move', label: '移动到...', icon: <SwapOutlined />, onClick: () => openMoveModal(doc) },
    ...(doc.share_token ? [{ key: 'revokeShare', label: '取消分享', icon: <StopOutlined />, onClick: async () => { await api.revokeDocShare(doc.id); message.success('已取消分享'); loadDocs() } }] : []),
    { type: 'divider' as const },
    { key: 'delete', label: '删除', icon: <DeleteOutlined />, danger: true, onClick: () => handleDelete(doc) },
  ],
})
```

**Step 5: 在 JSX 中添加移动 Modal**

在现有 `<UploadModal>` 之前（文件末尾附近）新增：

```tsx
<Modal
  title={`移动「${moveTarget?.title ?? ''}」`}
  open={moveModalOpen}
  onCancel={() => { setMoveModalOpen(false); setMoveTarget(null) }}
  footer={null}
  width={640}
>
  {collections.filter(c => c.id !== colId).length === 0 ? (
    <EmptyState icon={<FolderOutlined />} title="暂无其他集合" description="创建新集合后即可移动文档" />
  ) : (
    <Row gutter={[16, 16]} style={{ paddingTop: 8 }}>
      {collections
        .filter(c => c.id !== colId)
        .sort((a, b) => a.name.localeCompare(b.name))
        .map(c => (
          <Col key={c.id} span={12}>
            <div
              className="col-card"
              style={{ cursor: 'pointer' }}
              onClick={() => handleMove(c.id)}
            >
              <div className="body">
                <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 10 }}>
                  <div style={{
                    width: 32, height: 32, borderRadius: 6, background: 'var(--ink-50)',
                    border: '1px solid var(--border)', display: 'flex', alignItems: 'center',
                    justifyContent: 'center', color: 'var(--ink-500)', fontSize: 15,
                  }}>
                    <FolderOutlined />
                  </div>
                  <div style={{ fontSize: 15, fontWeight: 600, color: 'var(--ink-900)', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                    {c.name}
                  </div>
                </div>
                <div className="desc">{c.description || '暂无描述'}</div>
                <div className="meta">
                  <span>{c.doc_count ?? 0} 篇 · {relativeTime(c.updated_at)}</span>
                </div>
              </div>
            </div>
          </Col>
        ))}
    </Row>
  )}
</Modal>
```

需要在上方导入区确认 `Row, Col` 已从 antd 导入（检查现有 import 语句）。

**Step 6: 验证**

```bash
cd frontend && npx tsc --noEmit
# 预期: 无类型错误
```

**Step 7: Commit**

```bash
git add frontend/src/pages/CollectionDetail.tsx
git commit -m "feat: add document move UI — dropdown menu + collection card modal"
```

---

## Verification

### API 验证

```bash
# 启动后端
cd backend && uvicorn app.main:app --reload --port 8000

# 测试移动（需先登录获取 token）
curl -X POST http://localhost:8000/api/documents/1/move \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"collection_id": 2}'
# 预期: 200，返回更新后的 document JSON
```

### 前端验证

1. 打开集合详情页，点击文档右侧「···」→ 确认出现「移动到...」菜单项
2. 点击「移动到...」→ Modal 弹出，展示其他集合卡片
3. 当前集合不在列表中
4. 点击目标集合卡片 → Modal 关闭，Toast 提示成功
5. 文档从当前列表中消失
6. 切换到目标集合，确认文档存在且属性完整（标题、标签、备注）
7. 如有分享链接，移动后仍可访问

### 边缘情况验证

1. 只有一个集合时 → Modal 显示空状态「暂无其他集合」
2. 移动当前已选中的文档 → 右侧自动切换到空状态
3. 网络异常 → Toast 提示「移动失败，请重试」

---

## 关联文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/app/api/documents.py` | 修改 | 新增 `POST /documents/{id}/move` 端点 |
| `frontend/src/api/client.ts` | 修改 | 新增 `moveDocument` 方法 |
| `frontend/src/pages/CollectionDetail.tsx` | 修改 | Dropdown + Modal UI |
| `specs/004-doc-move/spec.md` | 已存在 | 需求规范 |
